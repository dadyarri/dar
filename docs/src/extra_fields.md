# Extra Fields

Metadata keys stored in entry `extra` strings are documented in:

- [v5 Extra Fields](./v5/extra_fields.md)
- [v6 Binary Format](./v6/format.md)

The v6 index extends the entry tail with an xattr blob but still retains the `extra`
string for text metadata and encryption-related fields.
