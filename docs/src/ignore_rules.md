# Ignore Rules

Ignore-rule handling is not version-specific.

The stable traversal behavior is documented in [v5 Ignore Rules](./v5/ignore_rules.md),
and the same `.gitignore` / `.darignore` logic continues to apply in v6 workflows.
